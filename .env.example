# Copy to .env and set your real Postgres password.
# URL-encode special characters in the password (e.g. @ becomes %40).
DATABASE_URL=postgresql+psycopg2://postgres:YOUR_PASSWORD@localhost:5432/food_delivery
